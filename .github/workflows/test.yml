name: Test

on:
  push:
  pull_request:

permissions:
  contents: read

env:
  ACTON_CHANNEL: ${{ (github.ref == 'refs/heads/stable' || github.base_ref == 'stable') && 'stable' || 'tip' }}

jobs:
  build-and-test:
    # Local push + fork PRs only (local PRs are skipped because push ran them).
    if: github.event_name == 'push' || github.event.pull_request.head.repo.full_name != github.repository
    runs-on: ubuntu-latest
    steps:
      - id: setup-acton
        uses: actonlang/setup-acton@v1
        with:
          channel: ${{ env.ACTON_CHANNEL }}
      - name: "Check out repository code"
        uses: actions/checkout@v4
      - name: "Check dependency consistency"
        run: make check-dep-consistency
      - name: "Cache acton stuff"
        uses: actions/cache@v4
        if: env.ACTON_CHANNEL == 'tip'
        with:
          path: |
            ~/.cache/acton
            out
          # Hacky cache update:
          # https://github.com/actions/cache/blob/main/tips-and-workarounds.md#update-a-cache
          key: acton-${{ github.run_id }}
          restore-keys: |
            acton
      - name: "Ensure generated code is up to date"
        run: |
          make gen
          git diff --exit-code
      - run: acton test
      - run: acton test perf
      - run: acton build --release
      - uses: actions/upload-artifact@v4
        with:
          name: sorespo-linux-x86_64
          path: out/bin/sorespo
      - run: make build-linux-aarch64
      - uses: actions/upload-artifact@v4
        with:
          name: sorespo-linux-aarch64
          path: out/bin/sorespo
      - run: make build-macos-aarch64
      - uses: actions/upload-artifact@v4
        with:
          name: sorespo-macos-aarch64
          path: out/bin/sorespo

  webui-assets:
    # Local push + fork PRs only (local PRs are skipped because push ran them).
    if: github.event_name == 'push' || github.event.pull_request.head.repo.full_name != github.repository
    runs-on: ubuntu-latest
    steps:
      - name: "Check out repository code"
        uses: actions/checkout@v4
      - name: "Set up Bun"
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.14"
      - name: "Ensure embedded Web UI assets are up to date"
        run: |
          make gen-webui
          git diff --exit-code

  release:
    # Only run on the main branch
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      # Required for uploading release assets
      contents: write
    # webui-assets gates the release so tip binaries can never ship a stale
    # embedded UI (committed webui_assets.act out of sync with webui/).
    needs:
      - build-and-test
      - webui-assets
    env:
      TAG: tip
    steps:
      - name: "Check out repository code"
        uses: actions/checkout@v4
      - name: Delete tag
        run: gh release delete ${{ env.TAG }} --cleanup-tag
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: "Download all binary artifacts"
        uses: actions/download-artifact@v4
        with:
          pattern: sorespo-*
      - name: "Compress binary artifacts"
        run: |
          tar -czf sorespo-linux-x86_64.tar.gz -C sorespo-linux-x86_64 sorespo
          tar -czf sorespo-linux-aarch64.tar.gz -C sorespo-linux-aarch64 sorespo
          tar -czf sorespo-macos-aarch64.tar.gz -C sorespo-macos-aarch64 sorespo
      - name: "Upload artifacts as release packages"
        uses: ncipollo/release-action@v1
        with:
          allowUpdates: true
          artifacts: "sorespo-*.tar.gz"
          draft: false
          prerelease: true
          name: ${{ env.TAG }}
          tag: ${{ env.TAG }}
          token: ${{ secrets.GITHUB_TOKEN }}
          replacesArtifacts: true

  net-test:
    # Local push + fork PRs only (local PRs are skipped because push ran them).
    if: github.event_name == 'push' || github.event.pull_request.head.repo.full_name != github.repository
    strategy:
      fail-fast: false
      matrix:
        # Fork PRs have no access to secrets / private registry, so only run with public images
        TESTENV: ${{ fromJSON(github.event_name == 'push' && '["quicklab","quicklab-xrd","quicklab-crpd","quicklab-notconf","quicklab-srl"]' || '["quicklab-notconf","quicklab-srl"]') }}
        ARCH: [linux-x86_64, linux-aarch64]
        exclude:
          # No Cisco IOS XRd support for aarch64 :/
          - TESTENV: quicklab
            ARCH: linux-aarch64
          - TESTENV: quicklab-xrd
            ARCH: linux-aarch64
    runs-on: ${{ matrix.arch == 'linux-x86_64' && 'ubuntu-24.04' || 'ubuntu-24.04-arm' }}
    permissions:
      contents: read
      packages: read
    env:
      IMAGE_PATH: ${{ secrets.IMAGE_PATH || format('ghcr.io/{0}/', github.repository) }}
    needs: build-and-test
    steps:
      - name: Set up env for containers
        run: |
          # IOS XRd
          sudo sysctl -w fs.inotify.max_user_instances=64000
          # cRPD
          sudo apt-get update
          sudo apt-get install -qy linux-modules-extra-$(uname -r)
          sudo modprobe mpls_router mpls_gso vrf
      - name: "Check out repository code"
        uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: sorespo-${{ matrix.ARCH }}
          path: out/bin/
      - run: chmod a+x out/bin/sorespo
      - name: "Check out licenses repo"
        if: ${{ matrix.TESTENV == 'quicklab' || matrix.TESTENV == 'quicklab-crpd' }}
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/licenses
          path: licenses
          ssh-key: ${{ secrets.LICENSES_PRIVATE_KEY }}
      # If we're using IMAGE_PATH=ghcr.io/... login to ghcr.io using GITHUB_TOKEN
      # TODO: what about other (external) registries that require login?
      - name: Login to ghcr.io
        if: ${{ startsWith(env.IMAGE_PATH, 'ghcr.io') && (matrix.TESTENV == 'quicklab' || matrix.TESTENV == 'quicklab-crpd' || matrix.TESTENV == 'quicklab-xrd') }}
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
      - name: "Start quicklab"
        run: |
          make -C test/${{ matrix.TESTENV }} start
      - name: "Configure quicklab"
        run: |
          make -C test/${{ matrix.TESTENV }} copy run-and-configure
      - name: "Get device configs"
        run: |
          make -C test/${{ matrix.TESTENV }} test-get-config
      - name: "Run quicklab tests"
        run: |
          make -C test/${{ matrix.TESTENV }} test-ping
      - run: |
          make -C test/${{ matrix.TESTENV }} save-logs
        if: ${{ always() }}
      - uses : actions/upload-artifact@v4
        with:
          name: logs-${{ matrix.TESTENV }}-${{ matrix.ARCH }}
          path: test/${{ matrix.TESTENV }}/logs
        if: ${{ always() }}
